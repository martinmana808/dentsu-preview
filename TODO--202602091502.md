# TODO 2026-02-09 15:02

## 1. Local Database & Hardcoded Records
- [ ] Create `src/lib/data.ts` (or similar) to store hardcoded data.
- [ ] Define `sizes` array with:
    - 945 x 315
    - 2160 x 3840
    - 900 x 200
    - 1024 x 768
    - 640 x 480
    - 640 x 960
- [ ] Define `ranges` array with:
    - <480px
    - 480px - 768px
    - 768px - 1024px
    - 1024px - 1440px
    - 1440px - 2000px
    - >2000px
- [ ] Add random overrides to sizes/ranges in the data structure to simulate a real DB.

## 2. Properties Panel Styling & Colors
- [ ] **Globals (Green)**:
    - Update Global tab border to green.
    - Update "Expand/Collapse all" buttons in Global section to green.
- [ ] **Ranges (Purple)**:
    - Update Range tab border to purple.
    - Ensure Range overrides use `text-purple-900 bg-purple-50` and `border-purple-200`.
- [ ] **Sizes (Orange)**:
    - Update Size tab border to orange.
    - Ensure Size overrides use `text-orange-900 bg-orange-50` and `border-orange-200`.

## 3. Fix Floating Menus (Overflow)
- [ ] Fix "Add Override" dropdown being cut off by `overflow-hidden`.
    - Soltuion: Use a portal (if available in Svelte/Bits UI) or adjust z-index/positioning context.
- [ ] Fix "Apply" dropdown on collapsed items being invisible.
    - Solution: Same as above, ensuring menus render outside the scroll container or have higher z-index escaping the clip.

## 4. Enhanced "Apply To" Logic
- [ ] Update "Apply" menu options to: Global, Range, Size.
- [ ] Implement multiple selection for Range and Size applications.

## 5. BEM Classes
- [ ] Add BEM classes to major components:
    - `AdCanvas` (.ad-canvas, .ad-canvas__element)
    - `PropertiesPanel` (.properties-panel, .properties-panel__header, .properties-panel__section)
    - `BreakpointsBar` (.breakpoints-bar, .breakpoints-bar__ruler)
    - `TopNavigation` (.top-nav)
    - `Toolbar` (.toolbar)
